*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing — never commit the upload keystore or its credentials.
keystore.properties
*.jks
*.keystore

# Certificate pins are environment-specific (tied to the not-yet-provisioned production host);
# gitignored so the placeholder values in the .example can never be mistaken for real ones.
cert-pins.properties
