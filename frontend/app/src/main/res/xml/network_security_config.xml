<?xml version="1.0" encoding="utf-8"?>
<!--
   Cleartext (HTTP) is permitted ONLY for local development hosts (the emulator's host alias
   and loopback). Every other destination must use TLS. This lets the app talk to a local
   HTTP backend during development while blocking plaintext traffic in production.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
