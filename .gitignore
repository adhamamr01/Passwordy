# Maven
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment
.env
*.env

# Local secret config — DO NOT COMMIT (real credentials).
# Commit the *.example templates instead; copy them and fill in real values locally.
backend/src/main/resources/application-local.properties
backend/src/main/resources/application-docker.properties

# Database
*.db
*.db-journal

# Kotlin/Gradle — ignore caches/outputs but COMMIT the wrapper + version catalog
frontend/.gradle/
frontend/build/
frontend/app/build/
frontend/gradle/.gradle/

# Compiled
*.class
*.jar
*.war
*.ear
# …but the Gradle wrapper jar MUST be committed — gradlew can't bootstrap without it.
!frontend/gradle/wrapper/gradle-wrapper.jar

# Claude Code — commit shared commands/skills, ignore transient/local state
.claude/worktrees/
.claude/settings.local.json