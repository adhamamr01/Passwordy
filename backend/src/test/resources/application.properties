# Test profile. The datasource is provided at runtime by the Testcontainers
# PostgreSQL container (via @ServiceConnection in TestcontainersConfiguration),
# so no datasource URL/driver is configured here — keeping tests off H2.
spring.application.name=passwordy
spring.jpa.hibernate.ddl-auto=create-drop
